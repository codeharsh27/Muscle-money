[project]
name = "muscle-money-ai-service"
version = "0.1.0"
description = "FastAPI AI service for structured finance education assistance."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.6",
  "pydantic-settings>=2.5.2",
  "google-genai>=0.1.0",
  "openai>=1.47.0"
]

[tool.ruff]
line-length = 100
